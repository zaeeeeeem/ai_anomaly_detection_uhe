# Medical Chatbot Portal

Phase 0 setup scaffold for the Medical Chatbot Portal project.

- Backend scaffold in `backend/`
- Frontend scaffold in `frontend/`
- Environment examples in `backend/.env.example` and `frontend/.env.example`
