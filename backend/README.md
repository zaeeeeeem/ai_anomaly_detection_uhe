# Backend

Scaffold created in Phase 0. See `../MEDICAL_CHATBOT_PLAN.md` for implementation details.

## Phase 6 - Integration Testing

### Create an admin user
```bash
python scripts/create_admin.py --email admin@example.com --username admin --password admin123
```

### Run a smoke test (requires server running)
```bash
./scripts/phase6_smoke.sh
```
